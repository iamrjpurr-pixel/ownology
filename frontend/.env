# Placeholder for Emergent deployer path expectation.
# This Vite+React app reads env from /app/.env. Frontend uses relative
# /api/* paths (no REACT_APP_BACKEND_URL needed) because Express serves
# the SPA from the same origin in production.
REACT_APP_BACKEND_URL=
