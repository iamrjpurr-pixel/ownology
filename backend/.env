# Placeholder for Emergent deployer path expectation.
# Real env vars for this app live in /app/.env (single root file — the
# backend is Express+tRPC at /app/server, not a separate /app/backend
# FastAPI project).
